import React from "react";
import "./Rendering.scss";

const Rendering = ({ todos, onRemove, onToggle }) => {
  return (
    <div className="Contents">
      <h1></h1>
    </div>
  );
};

export default Rendering;
